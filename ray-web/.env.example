# Copy to `.env` or `.env.local` in this folder. Vite loads these for `npm run dev` / `npm run build`.
# Leave unset for same-origin production (see `getApiBase()` in `src/api.ts`).

# If the API is on another origin (e.g. API subdomain), set:
# VITE_API_URL=https://ray.wright5.us
